// Package {{.PackageName}} provides some utilities for the {{.TypeName}} type
// that mirror those found in the built-in math package.
package {{.PackageName}}

// Code generated by generate/generate.go from math.go.tmpl. DO NOT EDIT.

{{if .Signed}}
// Abs returns the absolute value of x, or |x|. If |x| exceeds MaxValue,
// MaxValue is returned.
func Abs(x {{.TypeName}}) {{.TypeName}} {
	if x < 0 {
		if -x < 0 {
			// x == MinValue
			return MaxValue
		}
		return -x
	}
	return x
}
{{end}}

// Dim returns the maximum of x-y or 0.
func Dim(x, y {{.TypeName}}) {{.UnsignedTypeName}} {
	if y >= x {
		return 0
	}
{{if .Signed}}
	// Go guarantees 2's complement, so the conversion is well-defined.
	return {{.UnsignedTypeName}}(x - y)
{{else}}
	return x - y
{{end}}
}

// Max returns the larger of x or y.
func Max(x, y {{.TypeName}}) {{.TypeName}} {
	if x > y {
		return x
	}
	return y
}

// Min returns the smaller of x or y.
func Min(x, y {{.TypeName}}) {{.TypeName}} {
	if x < y {
		return x
	}
	return y
}
